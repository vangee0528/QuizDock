# 安全策略

QuizDock 团队非常重视系统安全。我们感谢社区用户与安全研究人员协助发现并负责任地报告潜在漏洞。

## 支持的版本

我们为以下活跃分支提供安全更新与缺陷修复：

| 版本   | 维护状态           |
| :----- | :----------------- |
| 0.2.x  | :white_check_mark: |
| < 0.2  | :x:                |

建议生产环境部署始终保持在最新稳定版本。

## 报告安全漏洞

若发现任何安全风险或潜在缺陷，请**避免通过公开 Issue 披露**。请通过负责任的漏洞披露流程反馈：

1. **GitHub 安全通告**：通过仓库的 [Security Advisories](https://github.com/vangee0528/QuizDock/security/advisories/new) 提交私密报告（推荐）。
2. **私人联系**：若无法使用 GitHub Advisories，请直接联系仓库主要维护者。

### 报告内容建议

为协助快速复现与评估，建议在报告中附带：
- 风险类型与受影响功能模块（如权限绕过、归档解压越界等）。
- 清晰的复现步骤或最小验证示例 (PoC)。
- 运行环境（操作系统架构、部署方式如 Docker/二进制）。

### 响应机制

维护者会尽快确认报告、评估影响、私下开发与验证补丁，并在修复版本发布后协调公开细节。

## 安全架构与防护机制

QuizDock 在系统核心设计上遵循纵深防御原则：

### 1. 题库包解析防护
`.qbank` 归档包解压与加载引擎内置完善的安全检查机制：
- **路径穿越防护**：解析时强制过滤符号链接、绝对路径及包含 `..` 的非安全相对路径。
- **解压资源限制**：限制解压体积、文件数量及单条目容量，防范畸形压缩包攻击。
- **结构与白名单**：仅允许加载预定的结构规范文件、Markdown 题目与常用静态图片格式（PNG/JPEG/GIF/WebP）。
- **原子事务与完整性校验**：入库前对所有条目执行 `checksums.sha256` 校验，并在单一 SQLite 事务内原子执行，异常时自动回滚。

### 2. 网络与访问凭据
- **最小暴露原则**：服务默认仅监听 `127.0.0.1` 本地回环接口。
- **会话安全性**：开启认证后，会话使用随机令牌和具有 `HttpOnly`、`SameSite=Strict` 属性的 Cookie；HTTPS 请求下同时设置 `Secure`。
- **生产环境建议**：公网部署时建议配置反向代理（如 Nginx、Caddy）终结 TLS/HTTPS，并结合标准网络防火墙策略保障主机安全。
