# 贡献指南

感谢你对 QuizDock 的关注与支持！无论是修复缺陷、改进用户体验、完善文档还是勘误题库，任何形式的贡献我们都由衷欢迎。

## 参与方式

1. **报告问题 (Bug Report)**：如果在使用中遇到异常，请通过 [Issue Tracker](https://github.com/vangee0528/QuizDock/issues) 提交，并尽可能附带详细复现步骤与运行环境。
2. **题目勘误 (Question Correction)**：若发现题干有误、答案解析存疑或配图错误，可通过 Issue 报告或直接提交 Pull Request。
3. **功能建议 (Feature Request)**：欢迎在 Discussions 或 Issue 中讨论新的练习模式、工具链需求或交互优化想法。
4. **提交代码 (Pull Request)**：欢迎直接提交 PR 协助我们修复问题或实现新特性。

## 本地开发工作流

### 环境准备

- **Go**: 1.27 或更高版本
- **Node.js**: 22+ 及 **npm**: 10+

```bash
# 克隆仓库
git clone https://github.com/vangee0528/QuizDock.git
cd QuizDock

# 初始化依赖
make setup

# 启动前后端联合开发环境
make dev
```

开发模式下，后端服务将运行在 `http://127.0.0.1:8765` 并反代 Vite 前端开发服务器（支持 HMR 热更新）。

### 自动化检查

在提交代码前，请确保通过全套静态检查与测试：

```bash
make check
```

`make check` 会自动执行 Go 代码格式化、静态分析、前后端单元测试及前端 TypeScript 类型检查。

## 代码与提交规范

### 1. 代码质量与设计原则
- **保持轻量与低耦合**：核心引擎聚焦于练习管理、题库解析与本地状态持久化，避免引入不必要的庞大外部依赖。
- **完善测试覆盖**：对解析器、存储层、核心状态机等关键路径的改动，请补充对应的单元测试。
- **数据库迁移规范**：
  - 数据库表结构变更必须在 `internal/database/migrations/` 下新增按序递增的 `.sql` 迁移文件。
  - 严禁修改或删除已有版本的迁移历史，以保障用户数据平滑升级。
- **格式规范演进**：`.qbank` 题库规范若发生不兼容变更，须递增 `schema_version` 并保持向后兼容性。

### 2. 题库维护与勘误准则
- **主键稳定性**：`bank_id` 与 `question_id` 是用户持久化刷题记录、错题本与个人收藏的全局唯一主键。**修正现有题目时严禁变更题目 ID**。
- **资源管理**：题目所引用的配图需统一收录在题库对应的 `assets/` 目录中，在 Markdown 中使用标准相对路径引用。
- **独立发布机制**：题库内容与主程序解耦。题库勘误合并后，更新对应题库 `manifest.json` 中的版本号并推送 Tag 即可独立发布，无需随应用程序发版。

### 3. Git 提交与 Pull Request
- 分支命名建议遵循 `feat/xxx`、`fix/xxx` 或 `docs/xxx` 格式。
- Commit 建议采用清晰的提交信息（如 `fix: 修复多选题解析异常`、`feat: 支持题目快捷搜索`）。
- 面向用户的重大变更或破坏性改动，请同步更新 `CHANGELOG.md`。

再次感谢你让 QuizDock 变得更好！
